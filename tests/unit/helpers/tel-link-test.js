import { telLink } from 'ember-personal/helpers/tel-link';
import { module, test } from 'qunit';

module('Unit | Helper | tel link');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = telLink([42]);
  assert.ok(result);
});
